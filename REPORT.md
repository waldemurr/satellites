# Отчёт

**Проект:** веб-сервис проектирования устойчивой спутниковой группировки и оценки доступности связи в северных районах
**Постановщик:** ИНТЦ «АКИД» (Андрей Цветков) · **Сроки:** 11–13 сентября 2026 г.
**Статус:** готово к защите. Сервис развёрнут, все пользовательские сценарии ТЗ выполняются.

---

## 1. Что сдано

Веб-сервис «загрузил — настроил — рассчитал — сравнил», закрывающий функциональные требования ТЗ:

| Требование ТЗ | Реализация |
|---|---|
| Загрузка/изменение конфигурации (п.1) | 5 исходных сценариев + загрузка своего JSON с понятными сообщениями об ошибках; редактор: очередь запуска, RAAN/фаза плоскостей, дальность ISL, угол места, периоды отказов КА и шлюзов; сохранение и сравнение вариантов, выгрузка/повторная загрузка |
| Моделирование покрытия и сети (п.2) | Расчёт положений КА (круговые/эллиптические орбиты, возмущения J₂), граф контактов, 3D-визуализация, временная шкала с анимацией, диаграмма доступности с перерывами и причинами |
| Маршрутизация и отказы (п.3) | Dijkstra по снапшоту (стратегии hops/latency/capacity), пересчёт при изменении t и доступности, классификация причины разрыва (4 типа по ТЗ), тумблеры отключения КА/плоскостей/очередей |
| Сравнение вариантов и рекомендации (п.4) | Сравнение двух сохранённых конфигураций: различия параметров, итоговые метрики, подсветка лучшего, текстовая рекомендация |
| Дополнительно | Monte Carlo (случайные отказы, распределение доступности), рейтинг критичности аппаратов, эксперименты с отключением, отчёты JSON/CSV/печать |

## 2. Ключевые результаты расчётов (горизонт 24 ч, шаг 120 с, цель ≥ 90 %)

| Сценарий | C65 | C70 | C72 | Вывод |
|---|---|---|---|---|
| 01. Полная группировка | 96,5 % | 98,3 % | 98,9 % | цель достигается |
| 02. Первая очередь (16/48) | 26,5 % | 15,8 % | 13,5 % | этап не самодостаточен |
| 03. Отказ 10 КА | 78,9 % | 81,4 % | 82,4 % | ниже цели, нужен резерв |
| 04. ISL 2000 км | 77,5 % | 63,9 % | 66,1 % | сеть чувствительнее к дальности ISL, чем к отказам |
| 05. Эллиптические орбиты (эксцентриситет 0.01) | 99,9 % | 100 % | 100 % | резерв проекта для северных пунктов |

**Рекомендация проекта** (минимакс-оптимизация, `OPTIMIZATION.md`): решена задача max_cfg min_{F, |F|=k} min_client availability. Базовая геометрия 3 плоскостей ΔΩ=60° оказалась минимакс-лучшей схемы Уокера; для гарантированного (worst-case) SLA 90 % при потере до 4 аппаратов принята конфигурация `06_optimal.json` — 64 КА (48 основных + 16 резервных: 8 в середины слотов, 8 дублей критичных окон), ISL 3500 км: номинал 97–99 %, worst-case 89,6 % при k=4, среднее 91 % при k=6. При бюджете 48 КА гарантия 90 % держится при потере до 2 КА; эллиптический вариант (05) — резерв по номинальной доступности (~100 %).

## 3. Архитектура и технологии

- **Бэкенд:** Python/Flask, NumPy (векторизованная баллистика: Кеплер + секулярный J₂), SciPy (max-flow). Расчёт полного горизонта доступности — ~1,5 с.
- **Фронтенд:** одностраничный интерфейс без сборки, Three.js r128 (локально, offline), 5 вкладок: Моделирование, Конфигурация, Эксперименты, Сравнение, Отчёты.
- **Развёртывание:** `docker-compose up` (nginx :3000 + бэкенд :5002) или `python backend/run.py` (:5002). Проверено в обоих режимах.
- **Качество:** smoke-тесты `test_api.py`, `test_modules.py` (включая физические проверки: сходимость с двухтельной задачей при J₂=0, регрессия узлов 0,39°/сут, ограничение перигея 500 км).

## 4. Риски и их закрытие

| Риск | Мера |
|---|---|
| Жюри загружает новый сценарий | строгая валидация с человекочитаемым списком ошибок; проверено на некорректных файлах |
| Нет интернета на площадке | все ассеты локальны (Three.js, текстуры), Docker-образы собираются заранее |
| Отказ демо-окружения | сервис поднимается одной командой; резервный запуск без Docker |
| Вопросы по точности баллистики | методичка DEFENCE.md: обоснование модели, сходимость с теорией, допущения |

## 5. Готовность к защите

- Сервис развёрнут и стабилен
- Демонстрационные сценарии прорепетированы (полная группировка, этап развёртывания, отказ аппарата, изменение проекта)
- Записана видео-демонстрация интерфейса (1–2 мин)
- План выступления на 4 минуты (SPEECH_PLAN.md)
- Методичка расчётной модели (DEFENCE.md), управленческая записка (REPORT.md), README
- Стресс-тесты пройдены, отчёт — `STRESS_TESTS.md` (284 зап/с, 0 ошибок; воспроизводимый скрипт `stress_test.py`)
- Минимаксная оптимизация конфигурации, отчёт — `ОПТИМИЗАЦИЯ.md` (скрипт `optimize_config.py`, сценарий `data/06_optimal.json`)
