# Отчёт по стресс-тестам

**Объект:** веб-сервис проектирования спутниковой группировки (API :5002, Flask threaded)
**Стенд:** AMD Ryzen 7 7735U (8 ядер / 16 потоков), 16 ГБ RAM, Linux
**Дата:** 12.09.2026 · **Скрипт:** `stress_test.py` (воспроизводимый прогон: `.venv/bin/python stress_test.py`)

Цель — установить пределы применимости сервиса: масштаб группировки, плотность временной сетки, конкурентная нагрузка, поведение при отказах и на некорректных входных данных. Все сценарии-нагрузчики генерируются скриптом (48–384 КА), загружаются через API и удаляются после прогона.

## 1. Масштаб группировки

Синтетические группировки 48/96/192/384 КА (3–24 плоскости, i = 87°, 550 км, ISL 3000 км), снапшот графа контактов на t = 43 200 с:

| N | Рёбер в снапшоте | calculate (мед) |
|---|---|---|
| 48 | 80 | 6 мс |
| 96 | 332 | 7 мс |
| 192 | 1168 | 15 мс |
| 384 | 4854 | 45 мс |

Снапшот (положения + O(N²) тест видимости по хордам + углы места) остаётся интерактивным до 384 КА: рост ~линейный-на-N², но абсолютные значения в десятках миллисекунд. Интерактивная временная шкала (1 запрос на кадр) работоспособна до ~200 КА.

## 2. Тяжёлые расчёты по масштабу (route-timeline, полный горизонт 24 ч)

| N | Шаг прореживания | Время | Доступность C65→G_MUR |
|---|---|---|---|
| 48 | 1 (720 отсчётов) | 0,45 с | 0.921 |
| 96 | 1 (720 отсчётов) | 0,95 с | 1.000 |
| 192 | 6 (120 отсчётов) | 2,9 с | 1.000 |
| 384 | 6 (120 отсчётов) | 11,9 с | 1.000 |

Вывод: полный таймлайн маршрута без прореживания практичен до ~100 КА; дальше интерфейс автоматически увеличивает `step_sample` — расчёт остаётся в пределах десятка секунд даже на 384 КА.

## 3. Тяжёлые эндпоинты на базовом сценарии (48 КА)

| Эндпоинт | Объём | Время |
|---|---|---|
| `/api/availability` | 720 шагов × 3 клиента × max-flow | 1,5 с |
| `/api/monte-carlo` | 100 испытаний | 1,0 с |
| `/api/criticality` | 60 снапшотов, перебор КА маршрута | 0,06 с |

## 4. Плотность временной сетки

Шаг 60 с (1440 отсчётов, вдвое выше спецификации): availability за 3,0 с (линейный рост от 1,5 с при 720 отсчётах), результат согласован (C65 = 0.923 против 0.965 на шаге 120 с — разрешение сетки влияет на гранулярность, но не на выводы).

## 5. Отказоустойчивость и входные данные

- **exclude 24 из 48 КА**: снапшот за 5 мс, активно ровно 24 — отключение через API не добавляет накладных расходов; файл сценария не изменяется.
- **Невалидный сценарий**: отклонён за 2 мс с HTTP 422 и структурированным списком ошибок — ошибочные загрузки не влияют на сервис.

## 6. Конкурентная нагрузка

**16 потоков × 20 смешанных запросов** (calculate + route, случайные t):

| Метрика | Значение |
|---|---|
| Пропускная способность | 284 зап/с |
| Ошибок | 0 из 320 |
| p50 / p95 / p99 | 55 / 73 / 81 мс |
| max | 86 мс |

**Тяжёлый расчёт под нагрузкой:** фоном идёт Monte Carlo на 200 испытаний (1,7 с), параллельно 16 запросов calculate — p50 = 24 мс, p95 = 31 мс, ошибок 0. Тяжёлые задачи (max-flow) отдаются SciPy и отпускают GIL; лёгкие запросы не блокируются (`threaded=True`).

## Выводы

1. **Номинальная конфигурация (48 КА, шаг 120 с)** — все расчёты в пределах 1,5 с, интерфейс полностью интерактивен.
2. **Запас по масштабу** — до ~200 КА без потери интерактивности, до 384 КА с прореживанием временной сетки.
3. **Устойчивость к нагрузке** — 284 зап/с без ошибок; перекрывает ожидаемую нагрузку защиты (один-два эксперта + демонстрация) с запасом на два порядка.
4. **Корректная деградация** — рост времени расчёта линеен по числу отсчётов и близок к квадратичному по числу КА, что соответствует теоретической сложности алгоритмов (max-flow, перебор пар); неприятных сюрпризов (память, деградация при исключениях) не выявлено.
5. Рекомендуемый операционный предел сервиса: **до 48 наземных узлов, до 400 КА, горизонт до 48 ч**; дальше — прореживание сетки и кэширование результатов availability (модуль готов к добавлению LRU-кэша без изменения API).
