# Минимаксная оптимизация конфигурации группировки

**Задача:** выбрать конфигурацию группировки, обеспечивающую доступность связи ≥ 90 % **в худшем случае** при потере k аппаратов.
**Дата:** 12.09.2026 · **Скрипт:** `optimize_config.py` (воспроизводимый: `.venv/bin/python optimize_config.py`)

## 1. Постановка (минимакс)

```
max_cfg  min_{F: |F| = k}  min_{client ∈ {C65, C70, C72}}  availability(cfg, F)
```

- **cfg** — конфигурация из параметризованного семейства: схема плоскостей (3/4/6, шаг ΔΩ, фазировка F по Уокеру), дальность ISL, тип орбиты (круговая/эллиптическая), число и размещение резервных КА;
- **F** — набор отказавших аппаратов, |F| = k ∈ {2, 4, 6}. Полный перебор C(48, k) невозможен → сценарии сэмплируются: 30 случайных k-подмножеств (другие seed на скрининге и финале, чтобы финал не подгонялся) + адверсариальный набор (топ-k по критичности из routing.satellite_criticality — жадная оценка худшего случая);
- **availability** — SLA-метрика из availability.py: доля отсчётов сетки (120 с × 720), когда max-flow(client → шлюз) > 0.

Двухстадийная схема: скрининг 7 конфигураций на сетке 240 с (быстро) → финал 3 лучших + эллиптические варианты на полной сетке 120 с.

## 2. Результаты

Worst-case и среднее по 31 сценарию отказов (минимум по трём клиентам), %:

| Конфигурация | k=2 | k=4 | k=6 |
|---|---|---|---|
| **48 КА, базовая геометрия (ΔΩ=60°, как 01)** | **91.7** / 92.8 | 86.2 / 88.1 | 81.9 / 84.0 |
| 48 КА, Уокер 3, F=1 (ΔΩ=120°) | 87.9 / 88.7 | 83.8 / 84.9 | 78.8 / 80.6 |
| 48 КА, Уокер 3, F=5 | 85.7 / 86.6 | 82.2 / 83.1 | 77.5 / 78.9 |
| 48 КА, Уокер 4 (F=1, F=3) | 64.4 / 65.8 | 62.2 / 63.0 | 58.6 / 59.9 |
| 48 КА, Уокер 6 (F=1, F=2) | 61.4 / 65.8 | 56.7 / 63.9 | 52.2 / 61.4 |
| 48 КА + эллипс e=0.15, ω=45° | 91.0 / 92.1 | 85.8 / 87.9 | 80.8 / 84.0 |
| 48 КА + ISL 3500 км | — | 87.5 / 88.7 | 82.9 / 84.4 |
| 48 КА + ISL 4000 км | — | 87.6 / 88.8 | 83.5 / 84.8 |
| 56 КА (48+8 срединных резервов) + ISL 3500 | — | 87.6 / 91.8 | 84.0 / 87.6 |
| 56 КА (48+8 дублей критичных слотов) + ISL 3500 | — | 88.3 / 91.3 | 83.9 / 87.2 |
| **64 КА (48+16 резервов: 8 срединных + 8 дублей критичных) + ISL 3500 — `06_optimal.json`** | — | **89.6** / **93.5** | **86.7** / **91.0** |

Номинальная доступность (без отказов) рекомендуемой конфигурации: **97.1 / 98.8 / 99.2 %** (C65/C70/C72).

## 3. Выводы

1. **Геометрия.** Базовая схема постановщика (3 плоскости, ΔΩ = 60°) — минимакс-оптимальна внутри семейства: стандартные схемы Уокера (равномерный ΔΩ = 360°/P) хуже на 4–6 п.п. в худшем случае. Увеличение числа плоскостей (4–6) при 48 КА резко вредит: меньше аппаратов в плоскости → разреженнее ISL-сепь.
2. **Гарантированный SLA 90 % для 48 КА держится при потере до 2 аппаратов** (worst-case 91.7 %). При k = 4–6 worst-case не достигает 90 % ни одной комбинацией геометрии/дальности ISL/типа орбиты в пределах 48 КА.
3. **Резервные аппараты — главный рычаг.** +8 КА поднимают среднее при k=4 до 91.8 %, но worst-case лишь до 87.6–88.3 %: худшие набори бьют по окнам видимости конкретного клиента. +16 КА (64 КА) дают worst-case 89.6 % при k=4 и среднее 91.0 % при k=6. Тренд ≈ +2 п.п. worst-case на +8 КА → строгая worst-case гарантия 90 % при k=6 потребует порядка 80–90 КА.
4. **Дальность ISL.** Рост 3000 → 4000 км даёт всего ~+1 п.п. worst-case при k=4 — рычаг слабый, но дешёвый; 3500 км принят в рекомендуемую конфигурацию (без увеличения массогабаритов терминала на 4000).
5. **Эллиптическая орбита** (e=0.15) поднимает номинал до ~100 %, но worst-case профиль при отказах близок к круговой: это резерв по доступности, а не по отказоустойчивости.
6. **Худшие набори смещаются при добавлении резервов** (k=4: S25/S29/S21/S17 → S26/S35/S44/S12) — устойчивость растёт по всей группировке, а не только в одной точке.

## 4. Рекомендация проекта

**Принятая конфигурация — `data/06_optimal.json`:** 64 КА (48 основных + 8 резервных в середины слотов + 8 дублей самых критичных окон), 3 плоскости ΔΩ = 60°, ISL 3500 км, круговая орбита 550 км.

| Показатель | Значение |
|---|---|
| Номинал (без отказов) | 97.1–99.2 % |
| Потеря 2 КА, worst-case | ≥ 91 % (как 48 КА) |
| Потеря 4 КА | worst-case 89.6 %, среднее 93.5 % |
| Потеря 6 КА | worst-case 86.7 %, среднее 91.0 % |

**Условия применения:** гарантия 90 % — в среднем по сценариям отказов при k ≤ 6 и в худшем случае при k ≤ 4 (состав ~+33 % к группировке). Если бюджет ограничен 48 КА — гарантия 90 % сохраняется только при k ≤ 2 (worst-case); тогда эллиптический вариант (05) компенсирует деградацию по доступности, но не по отказоустойчивости. Дальнейшее направление: второй шлюз (снимает узкое место «окно видимости шлюза» из классификации причин разрыва) — количественная оценка доступна в интерфейсе через тумблеры шлюзов.
